%-------------------------------------------------------------------------
% Resume document, dense single-column layout.
%
% Identical in content to resume.tex. It reads the same metadata and the same
% generated section manifest, so both PDFs are built from one resume_data.json
% and cannot disagree about a date or a bullet.
%
% The only difference is the style package and the type size. Compile whichever
% one suits the application, or compile both and choose from the output folder.
%-------------------------------------------------------------------------
\documentclass[letterpaper,10pt]{article}

\usepackage{../templates/jake}
\input{../configuration/metadata}

\setDocumentMetadata{\myName\ Resume}{\myName}

\begin{document}

  \makeHeader

  \input{../sections/_manifest}

\end{document}
